package com.example.jarvisai.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// JARVIS AI - OBSIDIAN & ELECTRIC BLUE PALETTE
// ==========================================

// Backgrounds & Canvas (Deep Pure Blacks)
val JarvisBlack = Color(0xFF04060A)             // Deepest abyss black
val JarvisBackground = Color(0xFF07090F)        // Primary screen background
val JarvisSurface = Color(0xFF0F1420)           // Card / Container surface
val JarvisSurfaceElevated = Color(0xFF151C2C)   // Higher elevation (dialogs, menus)
val JarvisSurfaceVariant = Color(0xFF1B2337)    // Input fields, pill tags, chips

// Electric Cyan & Arc Reactor Accents
val JarvisPrimary = Color(0xFF00E5FF)           // Electric Cyan (High focus)
val JarvisPrimaryLight = Color(0xFF84FFFF)      // Glow highlight
val JarvisPrimaryDark = Color(0xFF00B0FF)       // Secondary electric blue
val JarvisOnPrimary = Color(0xFF001F28)         // Contrast text on primary buttons

// Deep Royal Blues for message bubbles & hierarchy
val JarvisUserBubble = Color(0xFF1565C0)        // User bubble primary royal blue
val JarvisUserBubbleStart = Color(0xFF1565C0)   // User bubble top gradient
val JarvisUserBubbleEnd = Color(0xFF0D47A1)     // User bubble bottom gradient
val JarvisAssistantBubble = Color(0xFF101624)   // Assistant bubble elevated black

// Borders & Glassmorphism Glows
val JarvisBorder = Color(0xFF1E283D)            // Subtle futuristic border
val JarvisBorderSubtle = Color(0xFF141B2A)      // Low contrast divider
val JarvisBorderGlow = Color(0x3300E5FF)        // 20% alpha Cyan Glow

// Typography & Text
val JarvisTextPrimary = Color(0xFFF0F4F8)       // Crisp off-white display
val JarvisTextSecondary = Color(0xFF8A99B5)     // Muted slate grey
val JarvisTextTertiary = Color(0xFF53627E)      // Subtle metadata / timestamps

// System & Telemetry Accents
val JarvisAccentGreen = Color(0xFF00E676)       // Online status / Tok/s metrics
val JarvisAccentRed = Color(0xFFFF3D57)         // Stop / Warning / Delete
val JarvisAccentCyan = Color(0xFF00E5FF)        // Cyan highlight
val JarvisAccentOrange = Color(0xFFFF9100)      // Standby / Warning
val JarvisAccentGold = Color(0xFFFFD700)        // Gold / Amber indicator
val JarvisCodeBackground = Color(0xFF090D15)    // Code snippet container
